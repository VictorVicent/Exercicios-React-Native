import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  
  titulo:{
    fontSize: 30,
    color: 'black',
    textAlign: 'center',
    marginTop: 20
  },

  contador:{
    fontSize: 50,
    color: 'black',
    textAlign: 'center',
    marginTop: 10,
    marginBottom: 10,
  },

  separador:{
    marginTop: 10
  }

})

export {styles}