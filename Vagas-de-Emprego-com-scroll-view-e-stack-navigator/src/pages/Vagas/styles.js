import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  
  titulo:{
    fontSize: 40,
    color: 'black',
    textAlign: 'center',
  },
 nome:{
    fontSize: 30,
    margin:20,
    fontWeight: 'bold',
    textAlign: 'center',
  },
})

export {styles}